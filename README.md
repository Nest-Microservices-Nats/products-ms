# Products Microservices

## Dev

1. clone repository
2. execute `yarn`
3. copy `.env.template` file and rename `.env`
4. execute prisma migrations: `npx prisma migrate dev` and `npx prisma generated`
5. run `yarn run start:dev`